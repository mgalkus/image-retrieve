import React from "react";

const ErrorMessage = () => {
  return (
    <div className="ui message">
      <p>No image found with your keyword(s). Try other keywords!</p>
    </div>
  );
};

export default ErrorMessage;
